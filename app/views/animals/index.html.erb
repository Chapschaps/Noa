<h1>Trouvez votre nouveau compagnon !</h1>
<br>
<% @animals.each do |animal| %>
   <%= animal.name %>
  <%= animal.specie %>
  <%= animal.location %>
    <%= cl_image_tag animal.photo.key, height: 300, width: 300, crop: :fill %>

<% end %>
